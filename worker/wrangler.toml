name = "arabic-nlp-proxy"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# HF_TOKEN is set as a secret: wrangler secret put HF_TOKEN
# Never commit the token — it lives in Cloudflare's encrypted secret store only
